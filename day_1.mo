actor {

    // Challenge 1
    public func add(n : Nat, m : Nat) : async Nat {
        return(n + m);
    }

    // Challenge 2
    public func square(n : Nat) : async Nat {
        return(n * n);
    }
}